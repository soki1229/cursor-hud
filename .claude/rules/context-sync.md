---
paths:
  - cursor_hud.py
---

# Context Sync

After modifying `cursor_hud.py`, update these files if the change affects them:

- **`CLAUDE.md`** — architecture decisions, commands, conventions, or gotchas.
- **`.claude/rules/cursor-hud-code.md`** — section map (added/removed/renamed
  banners or key symbols), color thresholds, settings keys.

Skip for cosmetic changes or bug fixes that don't alter public API.
